version: '3.8'

services:
  parcelpilot-agent:
    build: .
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=gpt-4o
    volumes:
      - ./data:/app/data
      - ./parcelpilot.db:/app/parcelpilot.db